# Edit Component

The `edit` command launches an editor (VS Code) for the project.

## Usage

```bash
bmde edit [OPTIONS]
```

## Options

*   `-d`, `--directory PATH`: Project directory to open.
*   `-e`, `--environment [docker|host]`: Backend to use.
