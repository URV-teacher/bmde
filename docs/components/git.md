# Git Component

The `git` command wraps git operations, optionally routing traffic through a VPN.

## Usage

```bash
bmde git [COMMAND] [ARGS]...
```

## Features

*   **VPN Integration:** Connect to a FortiClient VPN before executing git commands.
*   **Credential Management:** Automatically handles authentication.
