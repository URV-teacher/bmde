# Build Component

The `build` command compiles NDS projects using `devkitARM`.

## Usage

```bash
bmde build [OPTIONS]
```

## Options

*   `-d`, `--directory PATH`: Project directory to build.
*   `-e`, `--environment [docker|host]`: Backend to use.
