# Patch Component

The `patch` command patches NDS ROMs with DLDI drivers for FAT filesystem support.

## Usage

```bash
bmde patch [OPTIONS]
```

## Options

*   `-f`, `--file PATH`: Path to the `.nds` file to patch.
*   `-e`, `--environment [docker|host]`: Backend to use.
